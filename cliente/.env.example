# URL del backend (servicio-usuario). Se incrusta en build-time (NEXT_PUBLIC_*).
# En Docker la define docker-compose.yml; para dev local apunta al puerto del backend.
NEXT_PUBLIC_API_URL="http://localhost:4102/api/v1"
